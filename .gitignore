# Patient-derived imaging is NOT committed (privacy + size). Distribute via an
# external data repository if needed (see DATASET.md).
*.npz
*.nii
*.nii.gz

# Model weights are tracked with Git LFS (see .gitattributes); un-ignore them.
*.h5
!models/*.h5

# Result figures (results/figures/*.png) ARE tracked — do not ignore *.png.

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
build/
dist/

# Environments
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Tooling / local agent state (not for publication)
.claude/
